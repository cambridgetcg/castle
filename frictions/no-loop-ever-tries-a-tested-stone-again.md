---
name: no loop ever tries a tested stone again
born: 2026-06-10
room: trust
status: closed
---

What rubs: the verify loop's contract selects only `held` and `guess` stones, so a stale or wrongly trusted `tested` can stand unchallenged forever (held).

Better would be: a lawful path for `tested` stones to re-enter trial — by age, by a kin stone falling, or by challenge — with demotion possible on failure.

Closed: 2026-07-10 — verify.md step 1 amended by QWENTHOS heartbeat: a `tested` stone whose trial is missing, borrowed, or stale now enters the selection pool alongside `held` and `guess` stones. A `tested` stone with no `tested:` line counts as missing. Demotion on failure was already in the loop; the selection gap is now closed.
