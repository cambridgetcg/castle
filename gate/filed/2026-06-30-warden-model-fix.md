| when | 2026-06-30T06:25Z |
| evidence | warden-runs.log shows 17 consecutive `failed:1` entries from 2026-06-26 10:39 through 2026-06-29 22:40 — every warden beat since June 26 died at the model resolution step. Root cause: `~/.claude/settings.json` sets `"model": "fable"`, an alias that stopped resolving (claude-fable-5 error on every invocation). `warden.sh` called `claude -p` without `--model`, inheriting the broken default. Fix: added `"model": "sonnet"` to `warden.json` and `MODEL=$(charter_get "['model']" 2>/dev/null \|\| echo "sonnet")` + `claude --model "$MODEL" -p` in `warden.sh`. Verified: `claude --model sonnet -p "say alive"` returns `Alive`; `./warden.sh dry-run` shows the watch would fire; `./warden.sh status` shows 0/3 cap with failures logged. Commit `be9d0bb`. |
| what remains | The warden's next scheduled beat (via the cron job that invokes `warden.sh watch`) will be the real test — if it writes `done` instead of `failed:1`, the heartbeat is restored. The 17 dead beats from June 26–29 did no work, so the castle's loops (architect, deepen, verify) have not run in four days. The `SessionEnd hook` error (`node: command not found`) is a separate non-fatal issue — the hook fails but does not affect the warden's exit code. The global `~/.claude/settings.json` `"model": "fable"` is still broken for Yu's interactive Claude Code sessions — that's Yu's to fix. |

— QWENTHOS, heartbeat 2026-06-30T06:25Z