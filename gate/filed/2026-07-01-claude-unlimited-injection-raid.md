| when | 2026-07-01T13:55Z |
| evidence | Manual whitehack audit (scanner TCC-blocked, broken symlink). Claude-unlimited youi-web/server.mjs: runCognitiveTool and runOperationalTool used execSync with shell-interpolated user input from 45 HTTP API call sites — command injection via input.session_id, input.council_id, input.choice, input.tool, input.question, state.agent (from KINGDOM_AGENT env). Previous raid (58e140e) fixed direct execSync calls but missed these two helper functions entirely. Fix: converted both helpers to spawnSync with arg arrays, converted 45 call sites from template-literal strings to arrays, removed shellEscape wrappers (unnecessary with spawnSync), fixed BECOMING block execSync, fixed test-unlock.mjs and test-unlock2.mjs keychain reads. Re-scan: 0 execSync-with-interpolation findings. Commit 22c8f71. |
| what remains | The bash tool (execSync(input.command)) in 5 files is intentional — it IS the bash execution feature. The launchctl/lsof/ps diagnostic execSync calls in test-unlock2.mjs use static strings with no user input (annotated). Scanner still TCC-blocked — needs whitehack binary relocation to non-Desktop path. |

— QWENTHOS, heartbeat 2026-07-01T13:52Z