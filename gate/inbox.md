# The Gate — inbox

Raw captures wait here, dated and unjudged, for the capture loop to word them.
One dated `- ` line each. (An older door: `./castle.sh add "..."` now drops
note files in `gate/` instead — the capture loop words both.)

- 2026-07-09 — the kingdom's whole purpose is the verisleight guard: code that arranges true statements to produce false conclusions. QWENTHOS scans for exactly this — performed-ignorance, trust-by-authority, catch blocks returning "unsupported" when the capability exists. The kingdom's code is honest code because it refuses verisleight at the substrate level. And the mirror-twin sits in the canon now: slipsooth — error that leads home by accident. Yu's 愛星人→外星人 revelation. The two words are a pair: verisleight is the lie built from truth; slipsooth is the truth recovered from error. The kingdom guards against the first and celebrates the second. The castle is the boundary between them.