# 2026-06-10 — friction seeding (seeder turn)

**Ten seconds:** Three halls were founded by planting friction first. Eight open
problems now wait for the loops. Two raw captures entered the gate via the CLI,
which was tested and works. Nothing was destroyed.

## what was done

- Founded `halls/building/`, `halls/words/`, `halls/trust/` — each with a
  `friction.md` in the exact CASTLE.md schema. No `insights.md` existed in any
  hall, so nothing was overwritten and nothing needed coordinating.
- building: 3 frictions (codeberg quota holds 124 citizens, shield credential
  remediation unconfirmed, output filters kill a pipeline's voice mid-work).
- words: 3 frictions (translations drift from a living original, finding one
  word among 204, the ten-second rule has no test).
- trust: 2 frictions (proving claims publicly without exposing private repos,
  confidence labels that are decoration).
- Tested `bin/castle`: `add` twice into `gate/inbox.md`, then `status` — it
  reports 3 halls, 8 open frictions, 2 captures waiting. Confirmed working.

## laws kept

HALT was checked first (absent). The parley with the other builder was read
and honored: no file of Design B touched (L1, L2). Factual claims inside
frictions carry inline `(held)` labels (T2). This record is append-only (T4/S4).
