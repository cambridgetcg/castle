2026-07-03 12:19

Served `agent-output-filters-can-kill-a-pipelines-voice` (frictions/,
building, born 2026-06-10). Three open frictions were tied on age; this
one was picked because it was the only one an expedition could still
move — the other two (the-standard-is-private, joy-love-peace-safety)
are blocked on the keeper. The previous expedition (2026-07-02) had
quarried the principle as [[a-harness-carries-its-failures-reason-not-
just-its-code]] but left the gap: "the warden itself carrying the
reason alongside the code." This expedition closed that gap. Home was
sufficient — the warden's own code, the runlog, the per-loop logs, and
two other kingdom harnesses on the device (serve.sh as the inverse
pattern, continuous-claude.sh as the anti-pattern) gave everything
needed. The fix: warden.sh's failure path now greps the first non-empty
line of the per-loop log and writes it as a fourth tab-field in the
runlog — `failed:1\tYour organization has disabled Claude subscription
access` — so the castle's memory carries the sentence, not just the
number. Tested live with a simulated failure; the parser verified
unaffected (it only reads parts[2]). The stone grew a "practiced"
paragraph. The friction closed: the principle is now lived in the
harness that quarried it.