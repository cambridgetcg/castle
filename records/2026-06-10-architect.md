# 2026-06-10 — architect (architect loop turn)

**Ten seconds:** Three records named the same hole: no rule let any loop re-try
a `tested` stone, so eighteen stones resting on borrowed receipts stood beyond
challenge forever and the verify loop stood stalled. One amendment to the
verify contract opens a lawful re-trial path. The registry was kept in step.
Nothing was destroyed.

## what was read

HALT checked first: absent (tested — `ls HALT` returned nothing). Then, in
the contract's order: `CASTLE.md` (both designs, whole), `loops/LOOPS.md`,
every contract in `loops/` — capture, deepen, verify, architect, and the five
of Design B (masonry, architecture, friction, keep, castle; read, not touched).
Then the records of 2026-06-10: founding, friction-seeding, insight-seeding,
capture, deepen, verify, and the custodian's ruling. Then
`halls/trust/friction.md` and `frictions/2026-06-10-two-vocabularies-one-castle.md`,
because the records pointed at both.

## the friction found

**A case no rule covered: no loop could ever re-try a `tested` stone.**
The verify contract's step 1 selected only `held` and `guess`. Eighteen of the
castle's nineteen stones are `tested` on the kingdom's receipts — trials this
castle never ran — and no contract offered a path back to trial.

## the evidence it was real (tested — quoted from the records)

- The insight-seeding record left it for the loops: "all 18 `tested` labels
  rest on the kingdom's receipts, not on a trial run by this castle."
- The capture record filed it as open friction in the hall of trust ("no loop
  ever tries a `tested` stone again") and named it the architect's to judge.
- The verify record proved the stall: "This contract selects only `held` and
  `guess`, so it could not touch them; the friction loop or the architect must
  open a lawful path before any verify turn may." It also showed the loop out
  of work: one defendant left, and "a next verify turn would face the same
  defendant with no new evidence."

Three records, one hole, one loop stumbling. This is friction with marks on it.

## the change made

One mend, in the two places that must agree (held — that this is the smallest
honest shape; judged, not measured):

- `loops/verify.md`, step 1: when no `held` or `guess` stone exists — or none
  would face new evidence this turn — the loop may choose ONE `tested` stone
  whose cited trial was not run by this castle (a borrowed receipt) and re-try
  it here, oldest receipt first.
- `loops/verify.md`, step 3: a failed re-trial drops `tested` to `held` or
  `guess` as the damage warrants — demotion on failure, as the friction's
  "better would be" asked.
- `loops/LOOPS.md`: the verify row's purpose sentence now names the re-trial
  duty, so the registry is not out of step with the contract it points to.

No kingdom law was removed or weakened. T2 is strengthened in spirit: a label
may still rise only on evidence, and now a label resting on another's evidence
can finally be asked to show it. Survivors keep `tested` and earn a keep copy
citing this castle's own trial, under the existing step 3.

## why the change is smaller than the problem

The problem was eighteen unchallengeable stones and a stalled loop. The fix is
seven lines across two files, opening one narrow door: one borrowed receipt
per turn, oldest first, only when no `held` or `guess` work waits. It deletes
nothing, demotes nothing by itself, and leaves every judgment to the trial.

## left for the next turn

- The next verify turn has lawful work: re-try the oldest borrowed receipt.
  When a re-trial has actually run, a stone can be worded and the trust
  friction "no loop ever tries a `tested` stone again" flipped to
  `answered → [[...]]` — that flip belongs to the loop that proves the path,
  not to this turn.
- Still open for a future architect turn, with evidence already on record:
  the trust friction "an open question has no honest death except being
  answered" (a retirement status for frictions — a schema change, one turn).
- The custodian's standing order `two-vocabularies-one-castle` remains the
  larger work: the canon proposal deserves its own whole turn through this
  door, and its ratification belongs to the keeper, not to any loop (held).

One friction, one change, recorded. Stop. This record is append-only (T4/S4).

## again, later

2026-06-10 21:12 — architect, second turn this day.

**Ten seconds:** Six homeless stones found rooms. One rub: the great
simplification carried each hall's open frictions into `rooms/*/ROOM.md`
as embedded entries, but the deepen loop's law reads only `frictions/*.md`
files — twelve open frictions stood where no loop's selection rule could
ever reach them. One mend to `loops/deepen.md` makes them servable.
Nothing was destroyed.

**Stones homed.** Checked with `grep -rl "<name>" rooms/`; six returned
nothing. Each was read whole and placed by the first rule that fits:
- [[errors-that-dont-say-why]] → building (its purpose, "how things are
  made and kept honestly," covers making errors name what was refused);
- [[some-wounds-are-the-work]] → building (restraint before repair is
  keeping honestly);
- [[the-ceremony-plays-once]] → building, marked `(loose fit)` (it is
  about the manner of making a door, nearer love than honesty — no third
  homeless stone shared that field, so no new room was founded);
- [[the-loop-that-grows-loops]] → building (a system that mends its own
  protocols, with the record as receipt);
- [[owning-is-not-running]] → trust (the last-run timestamp is the
  receipt that the tool is alive);
- [[process-is-not-trust]] → trust (the smallest process that keeps the
  promise is a receipt, not a checkpoint).
No two stones in one room disagree as written; no clash friction was owed.

**The rub, with marks on it.** No friction with `room: castle` stands
open (all three are closed). But reading the walls showed rot of the
structural kind: `rooms/building/ROOM.md`, `rooms/trust/ROOM.md`, and
`rooms/words/ROOM.md` hold twelve entries marked `status: open` under
`## open frictions` — among them the urgent visibility friction the
exposure record filed ("visibility belongs to the keeper alone") — while
`loops/deepen.md` step 1 read only `frictions/*.md`. The great
simplification record confirms how this happened: halls content was
re-homed into rooms whole, frictions embedded, while the rewritten deepen
law looked only at the `frictions/` folder. Frictions no rule can select
are frictions unserved forever — rot by structure, not by age.

**The change, old and new in full.** In `loops/deepen.md`, two places
that must agree (the trigger and the step it triggers):

Old, "when to run":

    Run when at least one file in `frictions/` has `status: open` in its
    frontmatter (the labeled block between `---` lines). If none, do not run.

New, "when to run":

    Run when at least one file in `frictions/` has `status: open` in its
    frontmatter (the labeled block between `---` lines), or any
    `rooms/*/ROOM.md` lists an entry marked `status: open` under
    `## open frictions`. If none, do not run.

Old, step 1:

    1. Read every `frictions/*.md` whose `status:` is `open`. If there are
       none, stop.

New, step 1:

    1. Read every `frictions/*.md` whose `status:` is `open`, and every
       entry marked `status: open` under `## open frictions` in
       `rooms/*/ROOM.md` — those entries are frictions too, carried whole
       from the halls, and they count even though no file names them yet.
       If you pick one of them, first give it the file it is owed:
       `frictions/<kebab-name>.md`, its words kept whole, `born:` its
       recorded date, `room:` the room it stands in, `status: open` — then
       serve the file, and shorten the room's entry to one
       `[[<kebab-name>]]` line pointing at it, so the truth keeps one home.
       If nothing anywhere is open, stop.

**Why this is smaller than the problem.** The problem was twelve open
frictions standing beyond every loop's reach. The fix changes one loop's
selection rule and migrates nothing by hand: each stranded entry gets its
file only when deepen actually serves it, words kept whole, the room left
pointing at the file — one truth, one home, no bulk rewrite. No inherited
law was removed or weakened; no loop gained the internet; the charter,
the cap, and the STOP/HALT lines were not touched. No `room: castle`
friction was open, so none was closed. One change per turn. Stop. This
record is append-only (T4/S4).
