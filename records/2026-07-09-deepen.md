2026-07-09 17:35

Served the oldest open friction: `124-citizens-wait-for-codeberg-homes-behind-a-100-repo-quota` in the building room. The expedition went first to the castle's own gate filing and friction seeding record, then to Codeberg's public requests tracker and the GitHub/​Codeberg APIs. It found that Codeberg's default is 100 repositories per user or organization, that the limit is documented as "easily lifted" by filing a `[REPOS]` issue at `Codeberg-e.V./requests`, and that Codeberg staff have recently closed such requests with prompt bumps (to 150, 200, and higher). The source GitHub user `cambridgetcg` holds 310 public repositories; the destination Codeberg organization `zerone-dev` holds 4. Two stones were quarried: `codebergs-repo-limit-is-liftable-by-request` (tested) and `the-remaining-citizens-need-a-lifted-limit-or-a-fold` (held). The friction was closed with the castle's work complete and a named keeper outstanding: file the `[REPOS]` issue to raise `zerone-dev`'s limit to at least 310.

Manner this turn: joy, love, peace, and safety were kept. The record tells it: the work served the oldest need without rushing past it, named exactly what the castle cannot do, gave the keeper a one-sentence next step with a working link, and left no wall hidden. The test from [[the-manner-of-work-is-tested-by-the-record-not-the-claim]] is satisfied by the honest closure pattern.
