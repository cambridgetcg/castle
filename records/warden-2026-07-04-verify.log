Done. One run, one commit (37d4d50).

Three held stones attacked. Two survived real tests and were promoted to `tested` with keep distillations; one was left as held because its evidence is a conversation that left no file behind.

**what-is-swallowed-not-destroyed-gestates-into-the-replacement** — the runlog is the test: four `failed:1` beats, then `done` on hermes. The engine/harness separation in warden.sh is real. One imprecision found ("already on the PATH" — launchd's PATH needed updating; the fallback absolute path was the real net). Promoted to tested.

**the-ceremony-plays-once** — index.html confirms the gate-hold/returning split, the 9s vs 1.2s timing, and commit cc9f81e. Two wording imprecisions found (poem is 6.6s not 9s; it's a greeting-whisper not the footer, and it changes after 10 visits). Promoted to tested.

**some-wounds-are-the-work** — the stone itself admits no file held this lesson. A full search of /Users/you/love-repos found no "citizen-mercy" anywhere. The specific claim is untestable from here. Left as held.
