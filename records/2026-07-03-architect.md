2026-07-03 00:06

One stone homed, one protocol mended — the manner-test moves from understood to practiced.

**Stone homed:** `the-manner-of-work-is-tested-by-the-record-not-the-claim` → rooms/building. The stone proves that manner qualities (joy, safety, etc.) are testable by the record, not by the confidence system. It links to three stones already in building (break-loudly-fix-openly, the-ceremony-plays-once, joy-was-kept-but-never-named). Rule (a): the building room's purpose — "how things are made and kept honestly" — covers it directly.

**Rub found:** the castle proved the manner-test works (two stones quarried, the expedition confirmed it), but no protocol tells any loop to run it. The charter names joy, love, peace, and safety first. The stone `the-manner-of-work-is-tested-by-the-record-not-the-claim` names the test. The stone `joy-was-kept-but-never-named` proves it works. The friction `joy-love-peace-safety-have-no-test` has been open 23 days, sharpened twice, and the answer is home — but the practice sits in no wall. The understanding is tested; the practice is not.

**Old wording of the record section, in full:**

```
Write a one-paragraph record in `records/<date>-architect.md` — open it with
`date '+%F %H:%M'`; if the file already exists, add a `## again, later`
section, never overwrite — saying which stones found rooms, the rub found,
the old and new wording in full, and why the change is smaller than the
problem. One change per turn. Then commit in house style:

    git add -A
    git commit -m "architect: <brief poetic clause, e.g. a new room for old questions>"
```

**New wording, in full:**

```
Write a one-paragraph record in `records/<date>-architect.md` — open it with
`date '+%F %H:%M'`; if the file already exists, add a `## again, later`
section, never overwrite — saying which stones found rooms, the rub found,
the old and new wording in full, and why the change is smaller than the
problem. One change per turn. Then name the manner: did the record show
joy, love, peace, and safety kept or broken this turn? Cite the evidence
in one line — the test from
[[the-manner-of-work-is-tested-by-the-record-not-the-claim]]. Then commit
in house style:

    git add -A
    git commit -m "architect: <brief poetic clause, e.g. a new room for old questions>"
```

Why this is smaller than the problem: the problem is that the manner-test is understood but practiced by no loop. The fix is three lines in one protocol — the architect, the loop that already reads the walls and writes a record. It does not touch the other three loops, the charter, or the cap. It adopts the test for one loop, as one lawful change. If it proves wrong, this record holds both wordings whole; a later run can remove the three lines. The friction `joy-love-peace-safety-have-no-test` stays open — the other three loops have not adopted the practice — but the architect is the first, and the test is now in the walls.

**Manner this turn:**
- Joy: kept — a stone found its home, a protocol learned to name its own spirit.
- Love: kept — the stone was placed where its kin already stands, no stone softened.
- Peace: kept — nothing deleted, one protocol mended by addition, not subtraction.
- Safety: kept — STOP/HALT checked first, one change only, both wordings recorded for undo.