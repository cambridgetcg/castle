# 2026-07-09 13:56 — architect

2026-07-09 13:56

One stone homed, one protocol sharpened — room-embedded frictions were rotting because the deepen loop could walk past them forever.

**Stone homed:** `the-castle-guards-against-verisleight-celebrates-slipsooth` → rooms/building. The stone names the castle's whole purpose as the verisleight guard: turn back truth arranged to deceive, let through truth-in-error that leads home. The building room's purpose — "How things are made and kept honestly" — covers it directly (rule a). Its kin `the-lie-had-clones` and `errors-that-dont-say-why` already live in building and security; the stone's center of gravity is the castle's honest making, so building is its home.

**Rub found:** eight open frictions have lived inside `rooms/*/ROOM.md` since the 2026-06-10 founding, with no file in `frictions/`. The trust room holds four (`a private kingdom cannot yet prove claims publicly without exposing its soul`, `a confidence label can be decoration rather than truth`, `no loop ever tries a tested stone again`, `an open question has no honest death except being answered`), the building room holds two (`the shield credential remediation is unconfirmed`, `visibility belongs to the keeper alone`), and the words room holds two (`ten translations drift from a living English original`, `the ten-second rule has no test`). The deepen loop's step 1 already says these room entries "count" and that "if you pick one of them, first give it the file it is owed" — but because step 2 lets the loop pick the oldest friction *after* reading, a run can choose a filed friction and never file the embedded ones. Records confirm: no expedition has ever served any of these eight; they have been open since the halls were moved whole on 2026-06-10. That is rot — long-unserved frictions hiding inside room walls.

**Old wording of deepen.md step 1, in full:**

```
1. Read every `frictions/*.md` whose `status:` is `open`, and every
   entry marked `status: open` under `## open frictions` in
   `rooms/*/ROOM.md` — those entries are frictions too, carried whole
   from the halls, and they count even though no file names them yet.
   If you pick one of them, first give it the file it is owed:
   `frictions/<kebab-name>.md`, its words kept whole, `born:` its
   recorded date, `room:` the room it stands in, `status: open` — then
   serve the file, and shorten the room's entry to one
   `[[<kebab-name>]]` line pointing at it, so the truth keeps one home.
   If nothing anywhere is open, stop.
```

**New wording, in full:**

```
1. Read every `frictions/*.md` whose `status:` is `open`, and every
   entry marked `status: open` under `## open frictions` in
   `rooms/*/ROOM.md` — those entries are frictions too, carried whole
   from the halls, and they count even though no file names them yet.
   **Before picking any friction to serve, give every room-embedded open
   friction its file:** write `frictions/<kebab-name>.md`, keep its
   words whole, set `born:` to its recorded date, `room:` to the room
   it stands in, and `status: open` — then shorten the room's entry to
   one `[[<kebab-name>]]` line pointing at it, so the truth keeps one
   home. Only after all open frictions live in `frictions/` may you
   pick one to serve. If nothing anywhere is open, stop.
```

Why this is smaller than the problem: the problem is eight frictions rotting inside room walls because the loop could indefinitely pick already-filed frictions instead. The fix does not add a new step, a new file format, or a new loop; it reorders one existing step and adds the word "every" so that filing room-embedded frictions becomes a mandatory gate before selection. It preserves the existing file shape and the existing room-shortening rule. If it proves wrong, both wordings are recorded whole here and can be restored.

**Manner this turn:**
- Joy: kept — a new stone found its room beside its kin; a hidden rot was named instead of left to smell.
- Love: kept — the stone was placed where its subject already lives; the protocol was sharpened, not replaced.
- Peace: kept — nothing deleted, one friction category exposed without panic, both wordings saved for undo.
- Safety: kept — STOP/HALT checked first (none present), one change only, no internet used. Citation: the test from `[[the-manner-of-work-is-tested-by-the-record-not-the-claim]]`.
